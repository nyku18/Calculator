//---------------------------------------------------------------------------


#pragma hdrstop

#include "Operation.h"

//---------------------------------------------------------------------------

#pragma package(smart_init)

Operation::Operation()
{
   for(int i = 0; i < 100; i++)
        {
                Result[i] = 0;
        }
        for(int i = 0; i < 100; i++)
        {
                FirstNumber[i] = 0;
        }
        for(int i = 0; i < 100; i++)
        {
                SecondNumber[i] = 0;
        }
}
