//---------------------------------------------------------------------------


#pragma hdrstop

#include "Inverted.h"
#include "Quotient.h"

//---------------------------------------------------------------------------

#pragma package(smart_init)

Inverted::Inverted()
{

}

int Inverted::Calculate()
{
        int DotPosition = 0;

        Quotient InvertedResult = Quotient();

        InvertedResult.FirstNumber = FirstNumber;
        InvertedResult.SecondNumber = SecondNumber;

        DotPosition = InvertedResult.Calculate();

        Result = InvertedResult.Result;
        return DotPosition;

}
